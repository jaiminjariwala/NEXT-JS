export const contactPageCode = `'use client';

import {
  useEffect,
  useLayoutEffect,
  useRef,
  useState,
  useCallback,
} from 'react';

// ─── Full source lives in:
// src/components/library/Contact/ContactPage.tsx
//
// The canvas preview renders the real component directly.
// Copy ContactPage.tsx into your project and wire up /api/contact
// to handle the FormData POST (from, message, attachments[]).
// /paper-clip.png must be present in your /public folder.
`;
