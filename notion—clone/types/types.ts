/* 
    The types folder is typically used in TypeScript projects to store type definitions, which help ensure type safety and better developer experience through IntelliSense in IDEs.
*/

export type User = {
    fullName: string;
    email: string;
    image: string;
}