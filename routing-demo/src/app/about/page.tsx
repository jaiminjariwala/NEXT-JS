// the pages metadata takes precedence over the layout metadata if both are defined, that is if both overlap.
export const metadata = {
  title: "About Jaimin"
};

export default function About() {
  return <h1>About Me</h1>;
}
