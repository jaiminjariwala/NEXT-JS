export default function ForgotPassword() {
    return <h1>ForgotPassword</h1>
}

// auth❌ but (auth)✅ folder! 
// This informs Next.js that auth folder should be treated as "Route-Group" and its name should be omitted from the URL path!